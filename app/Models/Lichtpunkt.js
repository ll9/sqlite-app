export default class Lichtpunkt {
    constructor(
        id,
        longitude,
        latitude,
        ort,
        straße,
    ) {
        this.id = id;
        this.longitude = longitude;
        this.latitude = latitude;
        this.ort = ort;
        this.straße = straße;
    }
}